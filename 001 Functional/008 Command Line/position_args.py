import argparse

parser = argparse.ArgumentParser(description="calculate the sum of two int")
parser.add_argument("a", help="first integer", type=int)
parser.add_argument("b", help="second integer", type=int)
print(arg)
print(f"sum of two integer is {arg.a + arg.b} ")
